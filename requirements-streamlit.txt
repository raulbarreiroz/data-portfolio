# Dashboard dependencies (use in a separate venv from requirements.txt)
# Example: python -m venv .venv-ui && pip install -r requirements-streamlit.txt
streamlit>=1.28.0
pandas>=2.0.0
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.9
