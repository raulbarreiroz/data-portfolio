# Loaded by Docker Compose for Airflow (committed defaults).
# Optional: create ".env" (gitignored) with the same keys to override, e.g. real AWS credentials.

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
S3_BUCKET=

# Leave S3_BUCKET empty to skip S3 uploads in the DAG.
