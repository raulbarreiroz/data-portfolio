# Copy to ".streamlit/secrets.toml" for local Streamlit (that file is gitignored).
# Or use the DATABASE_URL environment variable instead.

DATABASE_URL = "postgresql+psycopg2://app_user:app_pass@127.0.0.1:5434/portfolio"
